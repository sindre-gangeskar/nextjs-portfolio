import { Container, Box } from "@mui/joy";
export default function Navbar() {
	<Container sx={{ maxWidth: 800, maxHeight: 80, margin: "auto", backdropFilter: 'blur(8px)', fontFamily: 'var(--font-public-sans)' }}></Container>;
}
